

The user wants the plan PDF upload to be the FIRST step in project creation, so the AI can auto-extract project metadata (address, county, jurisdiction, trade type, etc.) from the title block — instead of forcing the user to type it all in manually before uploading.

Let me check the current creation flow to confirm where uploads happen today.

