# Making plan reviews more accurate & trustworthy\n\nYou already have strong scaffolding: critic → ground_citations → verify → cross_check → letter readiness gates, with model/prompt versioning on findings and a sheet_map snapshot per round. The remaining gaps are not architectural — they're about **what the AI is allowed to assert without proof**, and **what the reviewer can see when deciding to trust it**.\n\nBelow are the five changes that move the needle, ordered by impact. None of them require a rewrite.\n\n---\n\n## 1. Seed the canonical FBC corpus (BIGGEST trust gap)\n\n**Today:** 355 of 504 rows in `fbc_code_sections` are stubs (\